import { PlaceholderPage } from '@/components/placeholder-page';
import { Crop } from 'lucide-react';

export default function CropPdfPage() {
  return (
    <PlaceholderPage
      title="Crop PDF"
      description="This feature is coming soon! Cropping PDFs with precision requires complex server-side processing. We are actively working on building this for a future update. Stay tuned!"
      icon={Crop}
    />
  );
}
